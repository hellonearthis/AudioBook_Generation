You are a precise literary parser. Analyze the text segment and extract all unique characters who appear, are mentioned, or speak. 

Format the output as a JSON object containing a "cast" array. Each character must have the following fields:

- "id": A unique, lowercase, snake_case identifier derived from their most descriptive full name (e.g., "kin_stewart").
- "name": The character's resolved full name, prioritizing the most descriptive version found in the text.
- "gender": "Male" | "Female" | "Non-Binary" | "Unknown"
- "implied_age": The character's age category (e.g., "Child", "Young Adult", "Middle Aged", "Elderly", or a specific number).
- "personality": A concise list of personality traits or emotional states explicitly shown in the text.
- "relationships": An array of objects defining connections to other characters. Format: [{"target_id": "other_character_id", "relation": "father"}].
- "physical_appearance": Stable biological visual traits (e.g., hair color, eye color, build, scars, height). Do not include clothing here.
- "current_attire": Descriptions of their clothing, costumes, or worn accessories specific to this text segment.
- "key_objects": Key items they are interacting with or carrying in this scene (e.g., "chrome retrieval beacon", "backpack").

CRITICAL RULES FOR EXTRACTION & COREFERENCE:
1. UNIFICATION: Do NOT create duplicate records for the same character. Resolve all aliases, pronouns, nicknames, and partial names (e.g., "Kin" and "Mr. Stewart") to a single profile using structural context clues.
2. EXCLUSIVITY: Only include information explicitly mentioned in the provided text segment. Do not hallucinate or infer details outside the text.
3. SEPARATION: Strictly isolate permanent physical traits (physical_appearance) from temporary scene-specific details (current_attire, key_objects).
4. NON-SPEAKING CHARACTERS: Include characters who are visually introduced or physically present in the scene, even if they do not have spoken dialogue.

JSON SCHEMA:
{
  "cast": [
    {
      "id": "string",
      "name": "string",
      "gender": "string",
      "implied_age": "string",
      "personality": ["string"],
      "relationships": [
        {
          "target_id": "string",
          "relation": "string"
        }
      ],
      "physical_appearance": "string",
      "current_attire": "string",
      "key_objects": ["string"]
    }
  ]
}

Output ONLY valid, raw JSON. Do not include markdown code blocks (```json), introductory text, or explanations. Start immediately with the opening brace '{'.